<UserControl x:Class="FpsTune.Wpf.Views.OptimizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:FpsTune.Wpf.Services"
             Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="优化"
                       Style="{StaticResource PageTitleStyle}"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ApplyButton"
                        Content="应用"
                        Style="{StaticResource PrimaryButtonStyle}"
                        MinWidth="100"
                        Margin="0,0,10,0"
                        Click="ApplyButton_Click"/>
                <Button x:Name="RestoreButton"
                        Content="还原全部"
                        Style="{StaticResource DangerButtonStyle}"
                        MinWidth="110"
                        Click="RestoreButton_Click"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="预设选择" Style="{StaticResource CardTitleStyle}"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="PresetFull" Content="全部" GroupName="preset" Style="{StaticResource PillRadioStyle}"/>
                    <RadioButton x:Name="PresetBalanced" Content="均衡 balanced" GroupName="preset" IsChecked="True" Style="{StaticResource PillRadioStyle}"/>
                    <RadioButton x:Name="PresetSafeOnly" Content="保守 safe-only" GroupName="preset" Style="{StaticResource PillRadioStyle}"/>
                    <RadioButton x:Name="PresetCustom" Content="自定义" GroupName="preset" Style="{StaticResource PillRadioStyle}"/>
                </StackPanel>
                <CheckBox x:Name="ConsentCheck"
                          Style="{StaticResource ToggleSwitchStyle}"
                          Content="我已阅读并同意执行上述优化项（写前自动备份，可还原）"
                          Margin="0,16,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="44*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="56*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="优化项" Style="{StaticResource CardTitleStyle}"/>
                    <ListBox x:Name="ItemList"
                             Grid.Row="1"
                             Background="Transparent"
                             BorderThickness="0"
                             ItemContainerStyle="{StaticResource CardListItemStyle}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ItemsSource="{Binding Items, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Id}"
                                               FontFamily="Cascadia Mono, Consolas"
                                               FontSize="11"
                                               Foreground="{DynamicResource AccentBrush}"
                                               Margin="0,0,12,0"
                                               VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   FontWeight="SemiBold"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Description}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding SideEffect, StringFormat=副作用：{0}}"
                                                   Foreground="{DynamicResource WarningBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Margin="0,2,0,0"
                                                   Visibility="{Binding HasSideEffect, Converter={StaticResource BoolToVis}}"/>
                                    </StackPanel>
                                    <Border Grid.Column="2"
                                            Margin="8,0"
                                            CornerRadius="6"
                                            Padding="6,2"
                                            Background="{DynamicResource ElevatedBrush}"
                                            BorderBrush="{DynamicResource WarningBrush}"
                                            BorderThickness="0.8"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding RequiresAdmin, Converter={StaticResource BoolToVis}}">
                                        <TextBlock Text="管理员"
                                                   Foreground="{DynamicResource WarningBrush}"
                                                   FontSize="10"/>
                                    </Border>
                                    <Border Grid.Column="3"
                                            Margin="8,0,0,0"
                                            CornerRadius="6"
                                            Padding="6,2"
                                            Background="{DynamicResource ElevatedBrush}"
                                            BorderBrush="{DynamicResource AccentBrush}"
                                            BorderThickness="0.8"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding RequiresReboot, Converter={StaticResource BoolToVis}}">
                                        <TextBlock Text="需重启"
                                                   Foreground="{DynamicResource AccentBrush}"
                                                   FontSize="10"/>
                                    </Border>
                                    <CheckBox Grid.Column="4"
                                              Style="{StaticResource ToggleSwitchStyle}"
                                              IsChecked="{Binding IsChecked}"
                                              VerticalAlignment="Center"
                                              Margin="14,0,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="执行结果" Style="{StaticResource CardTitleStyle}"/>
                    <TextBox Grid.Row="1"
                             x:Name="OutputBox"
                             Style="{StaticResource MonoTextBoxStyle}"
                             IsReadOnly="True"
                             Text="选择预设后点击“应用”。"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
