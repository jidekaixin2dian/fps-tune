<UserControl x:Class="DeltaForceTune.Wpf.Views.OptimizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:DeltaForceTune.Wpf.Services"
             Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="优化"
                       Style="{StaticResource PageTitleStyle}"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ApplyButton"
                        Content="应用"
                        Style="{StaticResource PrimaryButtonStyle}"
                        MinWidth="100"
                        Margin="0,0,10,0"
                        Click="ApplyButton_Click"/>
                <Button x:Name="RestoreButton"
                        Content="还原全部"
                        Style="{StaticResource DangerButtonStyle}"
                        MinWidth="110"
                        Click="RestoreButton_Click"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="预设选择" Style="{StaticResource CardTitleStyle}"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="PresetFull" Content="full" GroupName="preset" Margin="0,0,24,0"/>
                    <RadioButton x:Name="PresetBalanced" Content="balanced" GroupName="preset" IsChecked="True" Margin="0,0,24,0"/>
                    <RadioButton x:Name="PresetSafeOnly" Content="safe-only" GroupName="preset" Margin="0,0,24,0"/>
                    <RadioButton x:Name="PresetCustom" Content="自定义勾选" GroupName="preset"/>
                </StackPanel>
                <CheckBox x:Name="ConsentCheck"
                          Content="我已阅读并同意执行上述优化项（写前自动备份，可还原）"
                          Margin="0,14,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="44*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="56*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="优化项" Style="{StaticResource CardTitleStyle}"/>
                    <ListBox x:Name="ItemList"
                             Grid.Row="1"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ItemsSource="{Binding Items, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0"
                                              IsChecked="{Binding IsChecked}"
                                              VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Id}"
                                               FontFamily="Cascadia Mono, Consolas"
                                               Foreground="{DynamicResource AccentBrush}"
                                               Margin="8,0,10,0"
                                               VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   FontWeight="SemiBold"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Description}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding SideEffect, StringFormat=副作用：{0}}"
                                                   Foreground="{DynamicResource WarningBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Margin="0,2,0,0"
                                                   Visibility="{Binding HasSideEffect, Converter={StaticResource BoolToVis}}"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="3"
                                               Text="管理员"
                                               Foreground="{DynamicResource WarningBrush}"
                                               FontSize="10"
                                               Margin="8,0"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding RequiresAdmin, Converter={StaticResource BoolToVis}}"/>
                                    <TextBlock Grid.Column="4"
                                               Text="需重启"
                                               Foreground="{DynamicResource AccentBrush}"
                                               FontSize="10"
                                               Margin="8,0,0,0"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding RequiresReboot, Converter={StaticResource BoolToVis}}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="执行结果" Style="{StaticResource CardTitleStyle}"/>
                    <TextBox Grid.Row="1"
                             x:Name="OutputBox"
                             Style="{StaticResource MonoTextBoxStyle}"
                             IsReadOnly="True"
                             Text="选择预设后点击“应用”。"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
