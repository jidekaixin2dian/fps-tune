# FPS 帧律 · 开发计划与产品路线图

> 更新：2026-09-01（v1.4.0 发版准备）。本文是产品方向与开发节奏的唯一权威说明，
> 与 catalog.json 的"优化项唯一数据源"定位平行。

## 产品定位

一句话：**Windows 玩家的系统层帧率调校台**——检测、解释、执行、还原，
全流程可逆、数据可验证，不碰游戏本身。

五条产品红线（任何开发不得违背）：

1. **安全闭环**：所有系统改动写前备份原值（含"原本不存在"状态），可逐项/一键还原，已达标项跳过不写。
2. **零侵入**：不修改游戏文件、不注入进程、不读游戏内存、不与反作弊交互、不伪装硬件、不关引导虚拟化。
3. **数据说话**：每项优化的价值主张都应可被 A/B 实验验证；解释不清收益的项不进 catalog。
4. **全 FPS 通用**：优化项是"系统层"的，不绑定特定游戏；游戏相关的只做路径级适配。
5. **可信透明**：所有改动有 JSON 审计记录；默认无遥测。

## 现状基线（v1.4.0，2026-09-01）

| 维度 | 状态 |
|---|---|
| 优化项 | 33 项（键盘×3/鼠标/网络×3/显示×5/系统×16/电源×2/存储等），3 预设 |
| 引擎 | C# 原生一统（GUI + `FpsTune.exe` 无头 CLI），catalog.json 单一数据源；PowerShell 引擎已于 v1.4.0 移除 |
| 界面 | WPF，无边框自绘窗口（DWM 圆角协作 + 内容圆角裁剪），深空蓝灰 + 靛青渐变设计体系 |
| 托盘 | 最小化到托盘、全局热键 Ctrl+Alt+F、优化/实验完成通知，设置页三开关 |
| 安全 | 自动备份/还原（含定向还原）、提权流程、写后校验、设置类文件原子写、旧数据自动迁移 |
| 发布 | Inno 安装包 + 版本化便携包 + SHA256 清单（单文件 149MB→66MB 压缩），GitHub Releases，版本三端单源 |
| 质量 | 57 项单测（含架构守卫与电源 GUID 回归守卫），CI 只读冒烟（exe CLI） |

## v1.1 —— 功能补全（已完成，v1.1.0 ~ v1.2.0）

1. **优化项分类分组**：catalog 增加 `group` 字段（键盘/鼠标/显示/网络/系统/电源/启动），
   优化页按分组渲染小节头，搜索框旁加分类过滤 chips。
2. **配置方案 Profiles**：把自定义勾选保存为命名方案（本地 JSON），
   支持导出/导入分享；为 v1.2 的"按游戏自动应用"打地基。
3. **托盘常驻**（v1.2.0）：最小化到托盘、全局热键呼出、优化完成通知（复用 PiWeb Desk 的成熟经验）。
4. **A/B 实验增强**（v1.2.0）：历史报告可视化（均值/1% low 折线对比，history.jsonl 只追加）。
   实验步骤向导化仍留待后续打磨。
5. **细节打磨**：检测页体检项可展开解释；首页快捷入口记住上次访问页。

## v1.2 —— 体验纵深（中期）

1. **应用内自动更新**：Release API 检查 → 下载 → SHA256 校验 → 静默执行安装器升级
   （当前只有"检查 + 跳转"）。前置依赖：代码签名证书（见风险）。
2. **硬件监控面板**：检测页实时小图表（CPU/GPU 温度、频率、占用），
   技术路线 LibreHardwareMonitor 或性能计数器，全程无注入、只读传感器。
3. **按游戏自动应用**：检测到绑定游戏进程启动时，自动应用对应 Profile
   （后台常驻进程 + 计划任务实现，遵守红线 2）。
4. **诊断报告导出**：一键打包系统信息 + 优化状态 + 备份清单，
   用于远程帮朋友排查问题（朋友测试流程的线上化）。
5. **优化项扩充（走"五处同步"清单）候选**：
   显示器刷新率/色域体检、音频独占排查、存储写入缓存策略、
   DirectStorage 就绪检查、BCD 深度项评估。

## v2.x —— 方向探索（长期）

1. **优化项插件化**：允许第三方以包形式贡献优化项（catalog 扩展 + 沙箱审计 + 显式启用）。
2. **多语言**：en-US 起步，资源字典化。
3. **性能基线对照**：匿名聚合"同硬件的典型帧率分布"（与红线 5 冲突，仅显式 opt-in，默认关闭）。
4. **官网与文档站**：安装率统计同样遵循 opt-in 原则。

## 工程治理（贯穿所有版本）

- **版本**：Directory.Build.props 单源；每次发布 = 打 tag + Release（安装包+便携包+SHA256）。
- **质量门禁**：catalog 一致性测试必须绿；涉及引擎的改动跑真实 apply→restore 闭环；
  界面改动本地启动逐页人工验收。
- **四处同步清单**（新增优化项）：catalog → C# apply/revert case（NativeOptimizationEngine）
  → 状态读取（DetectionService）→ BackupService 建档；测试口径同步。
- **已知债务**：无 UI 自动化测试（低优先，人工验收足够）；
  `fps-tune.ps1` / `legacy/delta-gui.ps1` 已于 v1.4.0 删除（CLI 由 exe 无头模式承接）。

## 风险与依赖

| 风险 | 影响 | 对策 |
|---|---|---|
| GitHub 账号被风控标记（申诉中） | 检查更新/对外下载不可用 | 申诉解封；过渡期可用网盘镜像发布 |
| 安装包未签名 | SmartScreen 劝退率高 | 评估 OV/EV 代码签名证书（年费成本） |
| 反作弊误判（理论上不会） | 用户封号风险 | 红线 2 是产品设计约束，永不让步 |
| 单人维护 | 节奏波动 | catalog 架构已把"新增项"成本降到数据+两处实现 |

## 版本历史

- v1.4.0（2026-09-01）：审查修复版——修复电源 GUID 错误（boost 模式误用设置项 GUID、
  删除编造的空闲降频对）、更新器指向死仓库、检测页 11 项状态漏读；新增 `FpsTune.exe`
  无头 CLI（-Detect/-Apply/-Restore/-ListRestore/-Version）替代并删除 fps-tune.ps1 与
  legacy/delta-gui.ps1；应用层 13 项修复（防重入、原子写、托盘句柄、注册表误写等）；
  单文件发布压缩 149MB→66MB。
- v1.3.4（2026-08-30）：发版准备——按游戏自动应用 Profile；动态只读硬件体检；备份还原安全闭环与更新器 SHA256 校验加固。明确不含 GPU 温度，显存监控未纳入本版。
- v1.3.3（2026-08-30）：发布版本——基于 v1.3.2 启动修复与 v1.3.1 体验改进，提供安装包、便携包及 SHA256 校验文件。
- v1.3.2（2026-08-30）：**紧急修复 v1.3.1 无法启动**——v1.3.1 在 App.xaml 里重复
  定义了 ScrollBar 隐式样式(与既有的圆角滚动条样式键冲突), 资源字典解析抛
  ArgumentException, 进程启动即终止。移除重复定义, 保留原有滚动条样式并补
  悬停/拖动变色反馈。v1.3.1 用户必须更新到本版
- v1.3.1（2026-08-30）：体验细节——全局细圆角滚动条（主题化, 应用于列表/文本框/
  设置页等一切可滚动区域）; 检测页根滚动（小窗口下内容不再被压缩裁切）;
  卡片阴影收敛至页边距内并支持低配模式关闭; 新增低配模式（减弱动效/关阴影/
  采样 1s→3s, 设置页开关）; 托盘图标随启动常驻（与最小化设置解耦, 不再出现
  "图标消失"）; 托盘右键菜单改为与主界面同款的圆角卡片样式; 极光开关即时生效;
  侧栏透明度提高让极光扫过全窗（消除内容区/侧栏割裂）
- v1.3.0（2026-08-30）：v1.2 体验纵深收官——应用内自动更新（检查→下载→静默安装，
  GitHub 不可达时优雅降级）、检测页硬件监控面板（CPU/内存/GPU 占用每秒采样迷你曲线）；
  游戏切换器（扫描全部已装 FPS 并可切换目标游戏，不再只认三角洲）；优化页双栏重排
  （预设/配置方案/执行结果移入左栏，优化项列表占满右栏，默认窗口可见条数 1→7+）；
  极光层上移为窗口级固定层（覆盖侧栏消除割裂，页面滑入时不再出现"左侧无光"）；
  托盘图标空白修复（app.ico 解包临时文件加载，失败时窗口保持任务栏可见）
- v1.2.3（2026-08-30）：**修复"首次打开页面右偏 24px"真根因**——初始首页不走滑入
  动画, RenderTransform 永久停在 X=24, 右侧页边距被吃掉(此前误判为 WinForms/DPI);
  极光层以加强版参数正式回归(DrawingBrush 合成进页面底色, 设置页可开关);
  托盘图标修复(图标句柄改取主窗口, 失败时不隐藏窗口); 新功能: 设置页"导出诊断
  报告"(打包概览/检测数据/脱敏设置/实验记录/错误日志/备份清单, 不含个人信息);
  卡片间距统一 14px
- v1.2.2（2026-08-30）：托盘重写为 Shell_NotifyIcon 纯 P/Invoke(移除 WinForms);
  附带 tools/AuroraDemo 极光层预览 demo
- v1.2.1（2026-08-30）：界面修正——优化页分类过滤与"优化项"标题叠加伪影
  （缺 Grid.Row）、"去素"氛围真正可见（页面平色背景盖住渐变的根因修复）、
  预设卡与配置方案卡间距、最大化按钮悬空笔画、搜索占位负 Margin 叠字缺陷
  （改为 DataTrigger 显隐）、关闭按钮红色悬停、标题栏按钮垂直居中、死样式清理
- v1.2.0（2026-08-30）：托盘常驻（最小化进托盘/Ctrl+Alt+F 呼出/完成通知）、
  A/B 历史趋势图（均值/1% low 折线）、窗口四角圆角根治（DWM DONOTROUND + 内容裁剪 + 描边色）、
  浅色主题氛围增强；**修复 LegacyMigrations 迁移自毁缺陷**
  （v1.0.2~v1.1.5 每次启动清空数据目录，跨会话备份丢失）
- v1.1.5（2026-08-30）：过渡版——界面氛围与执行结果富文本、4 项新优化（共 33 项）
- v1.1.0（2026-08-29）：优化项分类分组、配置方案 Profiles、游戏扫描扩容
- v1.0.5（2026-08-29）：导航焦点高亮修复、朋友测试图标重绘
- v1.0.4（2026-08-29）：键盘套件、PS 引擎 catalog 修复、全界面重构、设置页扩展
- v1.0.3（2026-08-29）：GPU 识别治本、文字渲染、开关化交互、鼠标加速项、开机自启
- v1.0.2（2026-08-27）：改名 FPS 帧律、全游戏通用化、catalog 单源、UI 现代化
- v1.0.1 及之前：三角洲帧律（单一游戏工具形态）
