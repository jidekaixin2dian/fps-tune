# 应用图标

2026-09-07 按用户明确要求，采用其提供的黑青速度表图标，替换标题栏、窗口、程序文件和托盘共用的资源。

源文件：`ChatGPT Image 2026年9月7日 11_25_46.png`，1254 × 1254，带透明通道。

源文件 SHA256：`EC2696419CF295C38E6AFCCFCED58437AA2EA38B0F0165A594F6CAE4D86E328A`。

未重绘或修改原图内容，仅缩放与格式转换。`app-icon.png` 为 256px；`app.ico` 包含 16、24、32、48、64、128、256px 七档 PNG 帧。可使用 `tools/Convert-AppIcon.ps1 -Source <原图路径>` 重建。

标题栏去除外加描边底座与分隔线，图标自然透明边缘直接呈现；“帧律”为主标题，FPS 为青色次级标识，版本号弱化。保留上一批最大化内边距和 52px 标题栏。

验证：Release 构建零警告、零错误；Windows 可读取 ICO 的 16px 帧，目视检查轮廓与指针可辨；实际应用窗口确认显示新图标和品牌排版。未替换已安装版本，也未修改用户原图。

## 托盘小尺寸适配

用户反馈实际托盘偏小且模糊后，确认旧托盘加载路径固定请求 16px；当前任务栏实际需要 24px。原图的留白和细节进一步减少了有效图形面积。

托盘现在使用单独的 `tray.ico`，保留黑青速度表辨识，移除细刻度与材质细节，扩大图形占比并加粗圆弧和指针。由 `tools/Build-TrayIcon.ps1` 从矢量几何分别绘制 16/20/24/28/32/40/48/64px 帧，不从小位图放大。标题栏和程序文件继续使用用户提供的完整图标。

加载尺寸通过任务栏 HWND 的 DPI 与 SM_CYSMICON 获取；设置、显示变化或 Explorer 重建时更新图标。替换成功后才释放原有句柄；临时文件使用独立随机名称并清理，避免多个预览互相覆盖。

验证：Release 构建通过，15 项相关回归测试通过；Windows ICO 读取分别确认 16/24/32px 帧尺寸正确，小尺寸图目视检查通过。本机只读调用确认托盘请求为 24px；独立 STA 探针验证 Shell_NotifyIcon 成功挂载内嵌新图标并清理。尚未在运行中切换显示缩放或重启 Explorer 验证刷新路径。
