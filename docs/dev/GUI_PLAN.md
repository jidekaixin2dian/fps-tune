# GUI 阶段规划（合规版）

> 目标：让不熟悉命令行的玩家也能安全地使用 fps-tune。
>
> **历史说明**：本文保留早期 GUI 规划供设计参考。当前 WPF `FpsTune.exe` 已覆盖
> 检测 / 优化授权 / A/B 实验 / 朋友测试 / 备份日志。视觉设计后续交给 V4 flash version，提示词见
> [GUI_DESIGN_PROMPT.md](GUI_DESIGN_PROMPT.md)。
> 原则：GUI 只做“调用引擎 + 展示结果 + 确认授权”，所有系统改动仍由
> `FpsTune.exe`（含进程内 `-Experiment` A/B 编排）完成，GUI 不绕过备份与还原机制。

---

## 盘点结果（2026-09-25，对照当时的 WPF 界面逐条核对）

> 目的：这份规划写于 WinForms 时代，很多条目早已落地或被更好的方案取代。
> 下面是**逐条核对后的结论**，只保留真缺口。核对方法：对每条承诺 grep 现界面与引擎，
> 确认"有没有真的做出来"，而不是看文档怎么说。

### ✅ 已实现（不必再做，正文对应条目可视为历史）

| 规划条目 | 现状 | 超出/差异 |
|---|---|---|
| §1 首页：检测与概览 | `HomeView` + `DetectView`（另有 `ConsoleView` 紧凑概览） | 优化项 **22 → 33**；只读体检从 3 项扩到 6+ 项（含刷新率、色彩配置） |
| §2 优化页：选择与授权 | `OptimizeView` | 预设 3 → 4（含自定义）；逐项显示副作用/管理员/重启；有同意勾选与执行记录 |
| §3 A/B 实验页 | `AbExperimentView` + 进程内 `ExperimentRunner` | 基线/CV 判定/候选组 keep-revert/报告全部具备 |
| §4 朋友测试页（Markdown 部分） | `FriendTestView` | 生成 Markdown 记录表 + 预览 + 打开输出目录 |
| §5 显示与 ICC 滤镜 | `DisplayQualityView` | **大幅超出规划**：除 ICC 外还做了 DLSS 预设覆盖、驱动 3D 设置（DRS）、数字振动 |
| §6 备份与审计页 | `BackupLogView` | 列出备份 / 按项还原 / 全部还原齐备 |
| §7 日志与导出页 | `BackupLogView` + `SettingsView` | 日志与 JSON 输出、导出日志、导出诊断报告 |
| 技术方案「阶段 2：C#/WPF 重写 + 安装包」 | 已完成 | WPF + .NET 10 + Inno Setup |
| 「视觉设计交给 V4 flash」 | 已完成 | 深空蓝灰 + 靛青，深/浅/跟随系统三主题 |

### ❌ 真缺口（规划承诺过，但界面里确实没有）

| # | 缺口 | 证据 | 影响 |
|---|---|---|---|
| **G1** | **按厂商生成「驱动内手动设置清单」**（§1） | 全仓库无此清单实现。A 卡/Intel 只在 `OneClickOptimizer` 里给了一句提示"请用显示页 ICC 滤镜" | 非 N 卡用户拿不到等价的驱动内调优指引，只能吃 ICC 兜底 |
| ~~**G2**~~ | ~~朋友测试缺 CSV 导出 + 「复制表格到剪贴板」~~ | — | **已作废**：朋友测试模块已于 2026-09-25 整体移除 |
| **G3** | **ICC 不支持用户自选 `.icc` / `.icm` 文件**（§5） | `DisplayQualityView` 只有 4 个内置预设，无文件选择对话框 | 有自己校色文件的用户无法套用；只能吃内置曲线 |
| **G4** | **备份页未展示 `.restored` 消费记录**（§6） | 引擎侧 `BackupRecord.Restored` 有完整机制，但 `BackupLogView` 不展示 | 用户看不出"这条备份已经被用掉了"，可能误以为还能还原 |
| **G5**（小） | **A/B 页缺 PresentMon 前置可用性检测**（§3） | 无前置探测；要等采样失败才在 `ExperimentRunner` 里提示 winget 命令 | 体验差一步（但最终提示内容与规划一致，且同样不代为安装） |

### 🗑 已过期（不要再按它做）

- 「阶段 1：PowerShell 5.1 + WinForms `delta-gui.ps1`」——已被 WPF `FpsTune.exe` 取代。
- 「22 项优化」「3 项体检」「预设 3 套」等具体数字——现为 33 项 / 6+ 项 / 4 套。
- 「M4（可选）：ICC 滤镜页」——已做，且不止于此。
- 正文 §1–§7 的**实现细节**（如"调用 `-Detect -Json`""弹出确认页"）保留作设计参考，
  但**以现界面为准**，不要照着重做。

---

## 明确不做

- 显卡型号伪装（已拒绝）
- 外置准星 / 悬浮窗 / Overlay（独立工具，不并入本项目）
- 游戏目录文件修改、进程注入、反作弊交互
- 代替用户下载或安装任何软件（PresentMon / VC++ / 驱动等）
- 自动勾选免责声明或代替用户确认

## 页面/模块

### 1. 首页：检测与概览

- 调用 `FpsTune.exe -Detect -Json`
- 展示：
  - CPU / 内存 / 显卡 / 系统版本 / 是否笔记本 / 是否管理员
  - 游戏路径（找不到时允许用户手动选择主程序）
  - 22 项优化当前状态（已达标 / 未应用）
  - 3 项只读体检结果（VC++ / 内存频率 / PCIe）
  - 显卡驱动内手动设置清单（按厂商生成）
- 提供“重新检测”按钮。

### 2. 优化页：选择与授权

- 三套预设卡片：`full` / `balanced` / `safe-only`
- 逐项列表：每项显示 id、作用、副作用、是否需要管理员、是否需要重启
- 用户勾选后点击“下一步”，弹出确认页：
  - 汇总将执行的项
  - 高亮需要重启的项
  - 高亮有副作用的项（wsearch-off / hibernate-off 等）
- 必须用户手动勾选“我已阅读并同意上述说明”后，才允许调用：
  - `FpsTune.exe -Apply -Items ... -Json`
- 执行后展示：成功 / 失败 / 跳过、备份文件路径、需重启项列表。
- 提供“一键还原”入口。

### 3. A/B 实验页

- 检测 PresentMon 是否可用（`Get-Command PresentMon` 或已知路径）
- 未安装时只展示官方安装命令，不代为安装
- 基线按钮：`FpsTune.exe -Experiment -Baseline -Json`（进程内 ExperimentRunner）
  - 显示平均 FPS / 1% low / P99 / 卡顿 / CV
  - CV > 0.05 时禁止继续，提示用户重新采样
- 候选组按钮：group-1 / group-2 / group-3
  - 显示每组 keep / revert 结果与原因
- 报告按钮：显示汇总表，并打开 CSV 路径
- 采样前提示：固定场景、画质、路线，保持前台，不要切窗口

### 4. 朋友测试页

- 集成 `friend-test.ps1` 的表单：
  - 自动读取 CPU / GPU / 内存 / 系统
  - 填写场景/画质、前后平均 FPS、前后 1% low、备注
- 生成 Markdown + CSV，支持“复制表格到剪贴板”和“打开所在文件夹”

### 5. 显示与 ICC 滤镜（阶段 2，可选）

- 列出显示器设备
- 允许用户选择 `.icc` / `.icm` 文件并应用到指定显示器
- 提供几个合规预设：
  - 标准（不改变）
  - 提亮 / 高对比 / 鲜艳（基于 ICC 或驱动级色彩设置）
- 不做游戏内画面滤镜、不注入、不 Overlay

### 6. 备份与审计页

- 列出 `%LocalAppData%\FpsTune\backup\` 下备份
- 展示每个备份可还原的项
- 支持按项或全部还原，展示 `.restored` 消费记录

### 7. 日志与导出页

- 展示最近一次命令的 JSON 原文
- 一键导出检测报告 / A/B 报告 / 朋友测试记录
- 一键打开状态目录

## 技术方案建议

- **阶段 1（历史）**：PowerShell 5.1 + WinForms 单文件 `delta-gui.ps1`，已由 WPF `FpsTune.exe` 替代。
  - 零依赖，Windows 10/11 自带
  - 仅封装现有 CLI，不引入新引擎逻辑
  - 文件保存为 UTF-8 BOM
- **阶段 2（可选）**：如果用户量上来，再考虑 C# / WPF 重写或加安装包
  - 仍然只做外壳，不改变核心脚本行为

## 里程碑

1. M1：检测页 + 优化授权页 + 还原入口
2. M2：A/B 实验页 + 朋友测试页
3. M3：备份审计页 + 日志导出页
4. M4（可选）：ICC 滤镜页
