# 开发计划 · 待办按 P0–P2 排序

> 最后核对：2026-09-24
> 本文是**待办总表**。功能细节看 `PLAN-0.1.2-features.md`；当前状态看 `docs/HANDOFF.md`；
> 纪律看 `AGENTS.md`。做完一项就把「状态」改掉，并在 `HANDOFF.md` §4 记一笔。

## 优先级定义

| 级 | 含义 | 开工条件 |
|---|---|---|
| **P0** | 正确性 / 可信 / 公开口径问题，或挡住发版的收尾 | 立刻做，不排队 |
| **P1** | 0.1.2 发版范围内的功能与承诺 | P0 清完或并行可开 |
| **P2** | 增强与后续版本，不挡 0.1.2 | 有余力再做 |

产品红线（`docs/ROADMAP.md` §产品定位）不可让步：安全闭环 / 零侵入 / 数据说话 / 全 FPS 通用 / 可信透明。

---

## P0 — 立刻做

| ID | 事项 | 为什么是 P0 | 验收 | 状态 |
|---|---|---|---|---|
| P0-1 | **M1 收尾目检**：GUI「显示与画质」三张卡（DLSS / 数字振动 / ICC）本机打开走查 | 代码已真机验证，但界面未过目；发 0.1.2 前必须人工看一眼（`PLAN-0.1.2-features.md` 里程碑约定） | 三卡布局/文案/状态/还原入口正常；深色/浅色不破 | **完成**（并修 2 处 UI 缺陷，`b7fe071`） |
| P0-2 | **真实 A/B 实验数据** | README 截图里的实验数字是历史/模拟状态，**不能用于宣传**（红线「数据说话」） | 跑一轮真机 `-Experiment`；报告中的收益可引用、可复现 | **用户取消**（2026-09-22：不要真实数据，直接发；**禁止编造收益数字**） |
| P0-3 | **统一「单文件 exe」口径** | `README.md`、`RELEASE.md` §4、实际 Release 资产曾三处不一致 | 三处一致：公开 **不提供单文件**（已选此口径）；`RELEASE.md` §4 = Setup + Portable + SHA256SUMS | **完成** |
| P0-4 | **0.1.2 发版门禁** | `VersionPrefix` 与已发布号区分；不发版则用户无法稳定获取 M1/M2 | 按 `RELEASE.md` 从干净 `finalSha` 构建 → GitHub Release 三资产 → Release Notes **不含编造的 FPS/收益数字** | **完成**（`v0.1.2-beta` @ `4e6b728`） |

---

## P1 — 0.1.2 范围与公开承诺

| ID | 事项 | 为什么是 P1 | 验收 | 状态 |
|---|---|---|---|---|
| P1-1 | **M3：DRS 二期设置项** | 同上 | 官方 ID 核对 + 推荐档（纹理高质量 / 透明度 2x→桌面 5070Ti 4x / 预渲染 1） | **完成**（已随 `v0.1.3-beta` 发布；推荐档 263/263 定稿） |
| P1-4 | **本机安装位 `D:\FpsTune` 升到发布构建** | 与 Release 对齐 | `0.1.3-beta`；`-Version` 自报一致，逐文件与发布 Portable zip 比对 7/7 | **完成**（2026-09-24，用户拍板；明确"不用备份"，整目录覆盖未跑安装器） |
| P1-2 | **界面文案国际化（issue #1）** | 公开承诺 | 设置页切换+持久化；框架文案英化 | **部分完成，已封存**（见下方说明） |
| P1-3 | **刷新 `docs/ROADMAP.md` 现状基线** | 基线段落仍停在 v1.5.0，与 0.1 线事实不符 | 基线改写到 0.1.2-beta / M1+M2 现状；与 `HANDOFF.md` 无矛盾 | **完成** |

---

## P2 — 后续增强（不挡 0.1.2）

| ID | 事项 | 说明 | 验收 | 状态 |
|---|---|---|---|---|
| P2-1 | **catalog 33 项说明文本英文翻译** | issue #1 明确拆出的后续项 | 英文 locale 下优化项说明无中文；中文原文不变；CLI `-Json` 键结构不变 | **完成**（2026-09-25；改法与踩坑见 `docs/dev/PLAN-P2-1-catalog-i18n.md`） |
| P2-2 | **A/B 实验报告可引用格式导出** | 真实数据（P0-2）落库后，补「给推广用」的导出（中英短报告） | 一键导出含测试条件/样本时长/对比指标；不含编造数字 | 未开始 |
| P2-3 | **GUI_PLAN 盘点** | 该规划写于 WinForms 时代，多数条目已落地或被更好方案取代 | 盘点 `GUI_PLAN.md` vs 现界面，只留真缺口 | **完成**（2026-09-25）：盘点结论写进 `GUI_PLAN.md`「盘点结果」；**7 个模块中 6 个已实现且多有超出**（显示页远超规划），查出 **5 个真缺口** → 已立为 P2-8 ~ P2-12 |
| P2-4 | **M4+：更多 DRS / 显示项**（在 M3 验证收益后） | 红线「数据说话」：先证明 M3 收益再扩 | 每项可 A/B；解释不清收益不进 catalog | 未开始 |
| P2-5 | **环境健壮性文档化** | 代理 shell 里的 Windows 变量名被 MSYS2 改成大写，曾被误记为"变量缺失导致 NuGet 炸"，每轮重复踩 | `AI-WORKFLOW.md` + `CONTRIBUTING.md` 各有一段可搜索的环境变量说明，含真实根因与正确用法 | **完成**（2026-09-24：`renv_arr[]` 根因 + 实测证伪旧记载） |
| P2-6 | **热门优化吸收（agent-reach 持续）** | 抖音/小红书/攻略站高频项：三重缓冲关、垂直同步关、各向异性 16x、着色器缓存、图像锐化、数字振动 55–60 | 每项过红线预审后再进 catalog / 显示页 | 调研已做一轮，待逐项落地 |
| P2-7 | **界面文案国际化收尾（原 OptimizeView chrome）** | 2026-09-25 审计发现：P1-2 当年只接了 35 处资源键却记为"完成"，实际全界面有 **1609 处**硬编码中文。已建机制（`Services/Str.cs` + 棘轮守卫）并转换到 **1000 处**（XAML 层已清零）。**用户 2026-09-25 决定暂停**：先把界面切回中文，不再投入 | 若重启：基线 `FpsTune.Wpf.Tests/I18nBaseline.txt` 即待办清单，按数量降序做；每改一处都要**真启动一次验证**（XAML 绑定错编译与单测都抓不到） | **暂停**（英文资源键全部保留，中文值即原文，切回 `zh-CN` 界面完全正常） |

| P2-8 | **按厂商生成驱动内手动设置清单（G1）** | GUI_PLAN §1 承诺过，未实现。非 N 卡用户现在只拿到一句"请用 ICC 滤镜" | 给出 A 卡/Intel 的驱动内手动调优清单（**只读指引，不改注册表**）；不承诺帧数 | 未开始 |
| P2-9 | ~~朋友测试补 CSV 导出 + 复制到剪贴板（G2）~~ | **作废**：朋友测试模块已于 2026-09-25 按用户要求**整体移除**（含 `FriendTestView`、`tools/friend-test.ps1`、`ScriptLocator`、`PowerShellRunner` 及其测试） | — | **不做**（模块已删） |
| P2-10 | **ICC 支持用户自选 `.icc` / `.icm` 文件（G3）** | 现只有 4 个内置预设，有校色文件的用户套不上 | 文件选择 + 应用到主显示器 + 可还原；多显示器仍不支持（已知） | 未开始 |
| P2-11 | **备份页展示 `.restored` 消费记录（G4）** | 引擎侧 `BackupRecord.Restored` 机制完整，界面没露出 | 列表标出"已用于还原"，避免用户误以为还能用 | 未开始 |
| P2-12 | **A/B 页加 PresentMon 前置可用性检测（G5）** | 现在是采样失败后才提示，体验差一步 | 进入实验页即探测并给出官方安装命令（**不代为安装**） | 未开始 |

---

## 已完成（勿再排队）

| ID | 事项 | 完成于 |
|---|---|---|
| done-1 | 脚本完整性校验根因修复（弃用 `Get-FileHash`，改 .NET SHA256） | `b7e5803` 等，已在 `main` |
| done-2 | 0.1 Beta 提升为 `main` 主线；1.x 冻结于 `legacy/1.x` | `1d9777c` / `legacy/1.x` |
| done-3 | `.gitattributes` 统一 LF（AI-WORKFLOW 里的旧待办已落地） | 仓库根 |
| done-5 | **一键优化定稿**：均衡 + DLSS K + 1070 Ti 档 3D + 电源方案 | `OneClickOptimizer`，264/264 | 2026-09-22 |
| done-6 | **P2-5 环境健壮性文档化**：查清 MSYS2 `renv_arr[]` 改名机制，证伪"变量缺失"旧记载 | `AI-WORKFLOW.md` §环境变量 / `CONTRIBUTING.md` | 2026-09-24 |

---

## 依赖关系（简图）

```text
P0-1/P0-2/P0-3 ──► P0-4 发 0.1.2 ──► P1-4 升 D:\FpsTune ✅（2026-09-24 完成）
P1-1 M3 ──► 已随 v0.1.3-beta 发布
P1-2 i18n ──► P2-1 catalog 翻译 ✅ ──► P2-7 全界面收尾（已暂停）
P2-3 GUI_PLAN 盘点 ✅ ──► 拆出 P2-8 ~ P2-12（5 个真缺口）
P2-6 热门优化项（调研完成）──► 逐项过红线后进 catalog
```

**建议顺序**：**P2-6（把调研过的热门优化项逐项落地 —— 最像"性能优化"、玩家收益最直接）**
→ P2-8（G1 非 N 卡用户的驱动调优指引，覆盖面最大）→ P2-9 / P2-11（都是小改动、体验直接）
→ P2-10 / P2-12。
P2-2 / P2-4 依赖真实 A/B 数据，而 P0-2 已被用户取消，**目前无前置数据，暂缓**。
P2-7（国际化）已按用户决定**暂停**。
**P0 与 P1 已全部收口；P2-3 盘点已完成并拆出 P2-8~P2-12。**

---

## 明确不做 / 不要误做的

- 不向 `legacy/1.x` 修 bug、发版、升级 .NET。
- 不改 CLI `-Json` 输出格式（有测试逐字节依赖）。
- 不读游戏内存、不注入、不与反作弊交互、不关引导虚拟化（红线「零侵入」）。
- 不把 README 模拟/历史实验数字当真实收益宣传。
- SettingID 未以 `nvapi.h` 核实前不写死进 catalog。
